name: M Monochrom BF561 Run Mask Runtime Object

on:
  workflow_dispatch:
  push:
    branches:
      - research/mm-sharpness-lutbank1a
    paths:
      - '.github/workflows/mm-bf561-runmask-runtime-object.yml'
      - 'tools/mm_bf561_runmask_runtime_object.py'
      - 'tools/mm_sharpness_consumer_trace.py'
      - 'tools/recover_leica_m9_mm_shared_key.py'
      - 'tools/pwad.py'

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Verify scripts
        run: python3 -m py_compile tools/mm_bf561_runmask_runtime_object.py tools/mm_sharpness_consumer_trace.py tools/recover_leica_m9_mm_shared_key.py tools/pwad.py
      - name: Install or build Blackfin objdump
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq binutils-multiarch curl xz-utils build-essential
          OBJDUMP="$(command -v objdump)"
          if ! "$OBJDUMP" -i 2>/dev/null | grep -Eiq 'bfin|blackfin'; then
            cd "$RUNNER_TEMP"
            curl --fail --location --retry 4 --retry-all-errors -o binutils-2.44.tar.xz https://ftp.gnu.org/gnu/binutils/binutils-2.44.tar.xz
            tar -xf binutils-2.44.tar.xz
            mkdir -p binutils-build && cd binutils-build
            ../binutils-2.44/configure --target=bfin-elf --disable-nls --disable-werror --disable-gdb --disable-gas --disable-ld --disable-sim
            make -j2 all-binutils
            OBJDUMP="$PWD/binutils/objdump"
          fi
          "$OBJDUMP" -i | grep -Eiq 'bfin|blackfin'
          echo "OBJDUMP=$OBJDUMP" >> "$GITHUB_ENV"
      - name: Download and decrypt canonical M Monochrom firmware
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/mmrunmask"
          curl --fail --location --retry 4 --retry-all-errors -o "$RUNNER_TEMP/mmrunmask/m9.upd" 'https://reddotforum.com/Firmware_files/M9%2C%20M9-P%2C%20M-E/m9-1_216.upd'
          curl --fail --location --retry 4 --retry-all-errors -o "$RUNNER_TEMP/mmrunmask/mm.upm" 'https://reddotforum.com/Firmware_files/M%20Monochrom/Mm-1_022.upm'
          python3 tools/recover_leica_m9_mm_shared_key.py "$RUNNER_TEMP/mmrunmask/m9.upd" "$RUNNER_TEMP/mmrunmask/mm.upm" --mm-out "$RUNNER_TEMP/mmrunmask/mm.dec" --meta-out "$RUNNER_TEMP/mmrunmask/recovery_meta.json"
      - name: Trace Run mask runtime object
        run: |
          set -euo pipefail
          python3 tools/mm_bf561_runmask_runtime_object.py "$RUNNER_TEMP/mmrunmask/mm.dec" --objdump "$OBJDUMP" --outdir "$RUNNER_TEMP/mmrunmask/derived"
          cat "$RUNNER_TEMP/mmrunmask/derived/MM_BF561_RUNMASK_RUNTIME_OBJECT.txt"
      - name: Publish derived evidence only
        uses: actions/upload-artifact@v4
        with:
          name: MMonochrom-BF561-RUNMASK-RUNTIME-OBJECT1A
          retention-days: 30
          if-no-files-found: error
          path: |
            ${{ runner.temp }}/mmrunmask/recovery_meta.json
            ${{ runner.temp }}/mmrunmask/derived/MM_BF561_RUNMASK_RUNTIME_OBJECT.json
            ${{ runner.temp }}/mmrunmask/derived/MM_BF561_RUNMASK_RUNTIME_OBJECT.txt
      - name: Remove firmware material
        if: always()
        run: rm -rf "$RUNNER_TEMP/mmrunmask/m9.upd" "$RUNNER_TEMP/mmrunmask/mm.upm" "$RUNNER_TEMP/mmrunmask/mm.dec"
