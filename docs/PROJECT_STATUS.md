# Project status

The canonical development branch is `research/mm-firmware-r0`.

This file exists on `main` only as a pointer. Research artifacts, scripts and evidence should land on the research branch first and be promoted deliberately.
